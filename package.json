{
  "name": "request-interval",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/request-interval.js",
  "scripts": {
    "build": "$(npm bin)/babel ./src/request-interval.js --out-file ./dist/request-interval.js"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  }
}
